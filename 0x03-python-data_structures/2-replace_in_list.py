#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    for element in my_list:
        if idx < 0 and > len(my_list):
            return (my_list)
        else:
            my_list[idx] = element
            return (mylist)
